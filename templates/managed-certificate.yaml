apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: terraform-enterprise-managed-cert
  namespace: {{ .Release.Namespace }}
spec:
  domains:
    {{- range .Values.managedCertificate.domains }}
      - {{ . }}
    {{- end }}
